telegram:
  token: "YOUR_BOT_TOKEN"
  allowed_user_id: 123456789

# Web dashboard API (optional)
# api:
#   port: 8080          # 0 = disabled (default)
#   token: ""           # optional bearer token for API auth
#   auth:
#     session_secret: "replace-with-at-least-32-random-characters"
#
#     # Pick exactly one external provider.
#     # oidc:
#     #   issuer_url: "https://auth.example.com/application/o/rcod/"
#     #   client_id: "rcod"
#     #   client_secret: "replace-me"
#     #   redirect_url: "https://rcod.example.com/api/auth/callback"
#     #   scopes: ["openid", "profile", "email"]
#     #   allowed_emails:
#     #     - "you@example.com"
#     #   allowed_groups:
#     #     - "rcod-admins"
#
#     # github:
#     #   client_id: "Iv1.xxxxx"
#     #   client_secret: "replace-me"
#     #   redirect_url: "https://rcod.example.com/api/auth/callback"
#     #   allowed_users:
#     #     - "octocat"
#     #   allowed_orgs:
#     #     - "zevro-ai"

rc:
  base_folder: "/home/user/Projects"

providers:
  claude:
    chat:
      permission_mode: "workspace-write"
    runtime:
      auto_restart: true
      max_restarts: 3
      restart_delay_seconds: 5

      # Session event notifications (optional)
      # notifications:
      #   progress_update_interval: 10m  # Periodic Telegram heartbeat for active sessions
      #   idle_timeout: 5m               # Notify when no output for this duration (0 = disabled)
      #   patterns:
      #     - name: "task_completed"
      #       regex: "(?i)(task completed|all done)"
      #       once: true
      #     - name: "error"
      #       regex: "(?i)(error:|fatal|panic:)"
      #       # once: false (default)

  codex:
    chat:
      permission_mode: "workspace-write"  # use bypassPermissions only for full local access
      model: "gpt-5.6-sol"
      reasoning_effort: "high"

  antigravity:
    enabled: true
    chat:
      permission_mode: "accept-edits" # Also: plan or bypassPermissions
      model: "gemini-3.6-flash-high"

# Optional remote machines/VMs. Build and install cmd/rcod-agent on each host.
# The dashboard adds each host as two providers: host / Codex and host / Antigravity.
# ssh:
#   hosts:
#     - id: vm-dev
#       name: Development VM
#       address: 192.0.2.20
#       port: 22
#       user: tomasz
#       identity_file: /home/tomasz/.ssh/id_ed25519
#       known_hosts_file: /home/tomasz/.ssh/known_hosts
#       base_folder: /home/tomasz/projects
#       agent_command: rcod-agent
